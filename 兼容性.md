# 配方查看与信息显示兼容性

本页记录 Deadly Monsters 26.2 对 JEI、REI、Jade 的兼容性判断、验证边界与处理原则

检查日期为 2026-09-06

## 结论

| 模组 | Minecraft 26.2 / NeoForge 状态 | Deadly Monsters 状态 | 是否需要专用适配 |
|---|---|---|---|
| JEI | 已有 26.2 NeoForge 构建 | 结构兼容，待联合验证 | 不需要 |
| REI | 已有 26.2 NeoForge 构建 | 结构兼容，待联合验证 | 不需要 |
| Jade | 已有 26.2 NeoForge 构建 | 基础信息结构兼容，待联合验证 | 不需要基础信息提供器 |

这里的结构兼容表示 Deadly Monsters 使用查看器能够直接理解的 Minecraft 标准注册和标准配方数据

结构兼容不等于已经完成对应模组的客户端与专用服务器联合实机验证，运行结果仍以 [`测试清单.md`](测试清单.md) 为准

## JEI 与 REI

Deadly Monsters 没有自定义 `RecipeType`、`RecipeSerializer`、配方容器或特殊工作台

现有 17 个配方全部位于标准配方数据目录，并继续使用 Minecraft 原生合成配方类型

因此 JEI 与 REI 不需要 Deadly Monsters 专用插件来解释这些配方，再引入一层可选 API 只会增加无意义的编译依赖、版本耦合和维护成本

专用服务器环境仍受查看器自身的配方同步机制影响，测试时应按照对应查看器的安装要求配置客户端与服务端组件，避免把客户端未收到的服务器配方误判为 Deadly Monsters 兼容问题

当前最稳妥的处理方式是保持标准 Minecraft 数据格式，并分别验证单人世界与专用服务器

## Jade

本模组方块和实体都通过标准 Minecraft / NeoForge 注册系统存在

Jade 显示基础名称与实体信息不需要 Deadly Monsters 专用信息提供器

当前没有需要额外展示的自定义机器能量、库存、流体、配方进度或方块实体私有状态，因此不添加空壳 Jade 插件

后续只有在确实出现需要额外展示的自定义玩法信息时才接入 Jade API，并只提供真实需要的信息

## 外部状态参考

- JEI https://modrinth.com/mod/jei
- REI https://modrinth.com/mod/rei
- Jade https://modrinth.com/mod/jade

## 兼容性原则

- 不为兼容模组添加硬依赖
- 没有必要时不引入可选 API
- 优先使用 Minecraft 标准注册、标准配方与标准资源格式
- 真正出现自定义配方、机器界面或额外提示信息后再增加最小专用适配
- 兼容层必须解决真实问题，不能只为了让仓库里出现模组名称
- 没有完成联合运行时不把结构兼容写成已经实机验证
