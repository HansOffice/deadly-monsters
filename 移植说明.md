# Deadly Monsters 1.12.2 → Fabric 1.21.1 移植说明

## 移植基线

原模组目标版本为 Forge 1.12.2

本分支面向 Minecraft 1.21.1 / Fabric 做原生回迁，不保留 26.2 API 兼容层，也不模拟 NeoForge 生命周期

目标工具链

- Minecraft 1.21.1
- Fabric Loader 0.19.5
- Fabric API 0.116.16+1.21.1
- Fabric Loom 1.17.20
- Gradle 9.5.1
- Java 21

## 完成状态

源码和资源已经回迁到 1.21.1，并在 GitHub Actions 使用目标 JDK 执行真实 `gradle build` 验证

实际客户端、专用服务器和玩法验证继续按 [`测试清单.md`](测试清单.md) 执行

仓库侧已完成

- [x] Fabric 构建系统与模组入口
- [x] 方块、物品、实体、声音与创造模式标签页注册
- [x] 原版贴图、声音、Logo 与 credits
- [x] 1.21.1 blockstate、方块模型与 `models/item` 物品模型
- [x] 原版功能方块与交互
- [x] 原版功能物品与投射物
- [x] 12 种怪物实体
- [x] 1.21.1 实体模型与 renderer 架构
- [x] 12 种怪物生成器物品
- [x] Fabric API 自然生成与 spawn placement
- [x] 实体与方块战利品表
- [x] 17 个标准合成配方
- [x] 轻量 JSON 配置与原版默认倍率
- [x] Java 21 / Gradle 9.5.1 / Fabric 1.21.1 构建验证

## 26.2 → 1.21.1 关键回迁

### Minecraft API

1.21.1 仍使用 `ResourceLocation`、旧 `MobSpawnType`、旧实体创建接口和旧方块交互签名

26.2 的 `Identifier`、`EntitySpawnReason`、`ScheduledTickAccess`、`InsideBlockEffectApplier` 等接口没有被做成兼容壳，而是直接退回 1.21.1 对应原生接口

### 实体渲染

1.21.1 还没有 26.2 的实体 RenderState 架构

模型回到 `HierarchicalModel<Entity>`，renderer 回到 1.21.1 的实体直接输入形式，自定义状态直接从实体已有同步数据读取

模型层使用 `EntityModelLayerRegistry`，实体 renderer 使用 `EntityRendererRegistry`

### 时间系统

1.21.1 使用 `ServerLevel#getDayTime` 与 `setDayTime`

Sunlight Drop 和 Haunted Cow 根据 24000 tick 昼夜周期工作，并尊重 `GameRules.RULE_DAYLIGHT`

### 透明方块

原版 Barbed Wire 明确使用 CUTOUT

Fabric 1.21.1 在客户端入口通过 `BlockRenderLayerMap` 显式注册 `RenderType.cutout()`，不依赖 26.2 才出现的自动透明层判断

### 物品模型格式

Minecraft 1.21.1 继续从 `assets/<namespace>/models/item` 读取传统物品模型

`assets/<namespace>/items` 属于 1.21.4 引入的新物品模型定义格式，因此 1.21.1 分支移除这批 26.2 遗留文件

### 生成器物品

Fabric 1.21.1 使用旧版 `SpawnEggItem(EntityType, primaryColor, secondaryColor, Properties)` 构造器

原模组的生成器物品拥有完整自定义纹理，因此两层标准 spawn egg tint 使用白色，避免对现有纹理重新染色

### 实体载入配置语义

Fabric 1.21.1 的 `ServerEntityEvents` 没有 26.2 的磁盘载入标记

本分支不为了一个缺失标记引入 Mixin，属性倍率应用时按实体当前生命状态处理，原本满血的实体在最大生命变化后保持满血，已受伤实体保留当前生命上限

### 世界和实体行为

- Mutant Steve 保留可选环境破坏攻击
- Freezer 保留减速、冻结与状态外观
- Entrail 保留非火焰受伤生成史莱姆
- Present 保留礼物笼与双 Creeper 行为
- Haunted Cow 保留错误武器触发昼夜变化
- Topielec 保留深水拖拽，同时限制高成本深水搜索刷新频率

## 配置

Fabric 版使用 `config/dmonsters.json`

保留配置概念

- 全局生命、攻击、速度倍率
- 每种怪物独立生命、攻击、速度倍率
- 每种怪物自然生成权重与禁用开关
- Mutant Steve `breakBlocks`
- Unborn Baby `blindness`
- Topielec `searchDistance` 与 `harpoonOnly`
- Haunted Cow `validWeapons` 与 `disableTimeChange`

配置在模组初始化时读取，修改后需要完整重启对应进程

## 可选兼容

本模组没有自定义配方类型、特殊工作台或 BlockEntity 数据面板

JEI、REI 和 Jade 不需要 Deadly Monsters 专用桥接层即可读取标准内容，联合运行仍按 [`兼容性.md`](兼容性.md) 和 [`测试清单.md`](测试清单.md) 复验

## 不再保留的旧依赖

1.12.2 的 Hostile Worlds Invasions、Mantle、CoroUtil 等旧版硬链接不作为 1.21.1 核心依赖恢复

Haunted Cow 继续支持配置物品注册 ID 与标准剑、弓判定，不绑定已经过时的旧 API

## 验证边界

仓库侧完成表示源码和资源已迁移，并能在目标工具链下构建打包

模型观感、粒子、自然生成分布、客户端与专用服务器实际运行、平衡性和可选模组联合运行仍属于运行时复验范围
