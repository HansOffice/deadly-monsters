# Deadly Monsters 1.12.2 → Fabric 26.2 移植说明

## 移植基线

上游模组目标版本为 Forge 1.12.2

本分支面向 Minecraft 26.2 / Fabric 做原生移植，不构造 NeoForge API 模拟层，也不引入跨加载器兼容框架

原版仍存在的内容尽量保留 `dmonsters` 注册 ID

目标工具链

- Minecraft 26.2
- Fabric Loader 0.19.5
- Fabric API 0.159.0+26.2
- Fabric Loom 1.17.20
- Gradle 9.5.1
- Java 25

## 当前状态

Fabric 平台层源码迁移已经完成

构建入口和手动测试清单已经切换到 Fabric 26.2

完整构建、客户端运行、专用服务器运行与实际玩法验证仍按 [`测试清单.md`](测试清单.md) 执行

在完成目标工具链构建前，本分支不声称已经通过发布验证

## 平台迁移

### 构建与模组入口

NeoForge ModDevGradle 与 `neoforge.mods.toml` 已替换为 Fabric Loom 与 `fabric.mod.json`

Minecraft 26.2 使用当前非混淆环境，因此本分支使用 `net.fabricmc.fabric-loom`，不加入 Yarn 映射或旧版重映射兼容层

通用入口使用 `ModInitializer`

客户端入口使用 `ClientModInitializer`

### 注册系统

NeoForge `DeferredRegister` 与 `DeferredHolder` 已替换为 Minecraft 原生注册表写入

为避免为了平台迁移大面积修改玩法代码，本分支增加极薄的 `RegistryRef<T>`，只保留现有 `.get()` 调用形状

这个包装器不模拟 NeoForge 生命周期，也不承担延迟注册，只用于缩小平台迁移的差异范围

方块与物品在创建前设置 26.2 所需的注册键，避免未绑定注册键问题

### 实体属性与生成位置

默认属性使用 Fabric API `FabricDefaultAttributeRegistry`

生成位置继续使用 Minecraft `SpawnPlacements`，保留原有地面、水中、高度、露天和蘑菇岛排除条件

### 自然生成

NeoForge 自定义生物群系修改器与 `data/dmonsters/neoforge` 数据已经从 Fabric 分支移除

Fabric 版使用 `BiomeModifications` 写入自然生成列表

雪地礼盒怪继续使用 `#dmonsters:snow_biomes`

异形水鬼继续使用 `#dmonsters:water_biomes`

其他普通陆地怪物继续使用主世界生物群系范围

每种怪物的默认权重和群组大小保持与 NeoForge 移植版一致

### 配置

NeoForge `ModConfigSpec` 已替换为轻量 JSON 配置

首次运行生成 `config/dmonsters.json`

不引入 Cloth Config、YACL 或其他第三方配置依赖

保留的配置概念

- 全局生命、攻击、速度倍率
- 每种怪物独立生命、攻击、速度倍率
- 每种怪物自然生成权重与禁用开关
- 突变史蒂夫 `breakBlocks`
- 腹中胎儿 `blindness`
- 异形水鬼 `searchDistance` 与 `harpoonOnly`
- 闹鬼牛 `validWeapons` 与 `disableTimeChange`

生成权重会在 Fabric 生物群系修改注册时读取，修改 `spawnRate` 或 `disabled` 后需要重启世界或服务器

### 事件

实体载入使用 Fabric `ServerEntityEvents.ENTITY_LOAD`

通过 Fabric `EntityLoadData` 保留新生成实体与磁盘载入实体的生命处理差异

玩家攻击实体使用 `AttackEntityCallback`

异形水鬼的 `harpoonOnly` 可以通过回调返回失败直接阻止不符合条件的攻击

闹鬼牛的昼夜变化逻辑继续在服务端处理

### 客户端渲染

模型层使用 Fabric API `ModelLayerRegistry`

实体渲染器使用 Minecraft 当前 `EntityRenderers.register`

12 套实体模型、渲染状态、渲染器与两个投射物渲染逻辑不为了平台迁移重新实现

## 玩法与资源

Fabric 分支继续复用已经完成的 Minecraft 26.2 玩法迁移

- 12 种原版怪物实体
- 12 种刷怪蛋
- 原版玩法方块和特殊交互
- 强化钢筋、四种鱼叉、幸运蛋、达贡和怪物功能掉落物
- 两种投射物
- 原版贴图与声音
- 17 个标准合成配方
- 实体与方块战利品表
- 当前方块状态、模型和物品定义数据

## 兼容性与现代化处理

### 配方查看器与信息显示模组

JEI、REI、Jade 均已有 Fabric 26.2 支持

本模组没有自定义配方类型、特殊配方菜单或自定义容器，17 个原版配方继续使用 Minecraft 标准配方体系，因此 JEI 与 REI 在数据结构上不需要专用适配插件

Jade 可以通过标准注册信息读取本模组方块与实体，基础显示不需要额外信息提供器

这里的结论属于结构兼容，单人世界与专用服务器的联合运行结果仍按 [`测试清单.md`](测试清单.md) 验证

详细记录见 [`兼容性.md`](兼容性.md)

### 世界时钟

Forge 1.12.2 使用可配置整数 `dayLengthTicks` 并直接修改世界时间

Minecraft 26.2 使用数据驱动的世界时钟与时间标记

因此数字 `dayLengthTicks` 配置已退役，日光降临与闹鬼牛继续使用当前主世界时钟的昼夜标记

### Hostile Worlds Invasions

旧版可选 Hostile Worlds Invasions 检查 `disableTimeChangeInvasions` 不再保留，因为当前移植没有对应 26.2 依赖或联动契约

### Tinkers' Construct / Mantle / CoroUtil

旧版构建期与运行期联动不再作为核心移植依赖

闹鬼牛仍支持可配置物品注册 ID 与标准剑、弓判定，不再硬链接已经过时的旧 API

### 异形水鬼搜索频率

1.12.2 的深水搜索计时会让高成本搜索近似每游戏刻执行

26.2 移植保留拖向深水的可见行为，同时限制搜索刷新频率，不复制原版性能问题

### 旧元数据

已经移除的元数据物品与方块映射到当前独立 ID，包括鱼类、黏土球、烟花、染料和彩色玻璃

## 验证边界

源码迁移完成表示 NeoForge 专属平台入口已经由 Fabric 对应机制替换，且资源与玩法逻辑保持在同一套 26.2 数据基础上

构建成功、客户端与专用服务器可启动、模型对齐、粒子、自然生成分布和实际玩法仍必须通过 [`测试清单.md`](测试清单.md) 逐项确认

GitHub Actions 继续保持手动触发，不为 Fabric 分支恢复每次推送自动构建
